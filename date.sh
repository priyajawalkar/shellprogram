#!/bin/bash
year=`date + %Y`
month=`date + %M`
day=`date + %D`
hour=`date +%H`
minuts=`date +%M`
seconds=`date +%s`
echo `date`

echo "current date is $day -$month -$year"
echo "current time is $hour -$minuts -$seconds"

